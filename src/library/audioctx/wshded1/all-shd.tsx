

/* eslint-disable import/first, @typescript-eslint/no-unused-vars    */

import SS from "lodash" ;
import Immutable from "immutable";
import * as Collections from "library/util-immutable-datastructure" ;
import * as Math2 from "library/util-math-max" ;



export {} ;













const values = (
  (
    <A1 extends object , A2>(obj: A1 & { [key: string]: URL ; } ) => (
      obj
    ) 
  )({
    GAINOF     : new URL(("shd-gainof.mjs"     ), import.meta.url, ) ,
    FREQSHFT   : new URL(("shd-freqsh1.mjs"    ), import.meta.url, ) ,
    // GAINOF : new URL(("shd-gainof.mjs"     ), import.meta.url, ) ,
  })
) ;
export default values ;
console["log"]({ values, }) ;






